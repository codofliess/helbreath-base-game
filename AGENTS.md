# Agent notes — helbreath-base-game

Fail-closed for Cloud Agents. Load skill **Coding LAWS fail-closed** (`sand-workflow coding-laws-fail-closed`). Pointer only — do not copy the book/owner matrix here (desync risk).

Gates without this `AGENTS.md` are theater (Cruchi NO-GO).

## Fail-closed (always)

- emitir=false
- zero secrets in chat
- Extra=0 amounts
- one owner per change
- each data law names book+owner (named in the skill, not pasted here)
- cotizar≠emitir
- one token=one book

## This repo

- Wallet/auth #33 fail-closed (no secret dumps; wallet claims required).
- Playtest ≠ chassis (ops/playtest recipes are not the game chassis).
- No SEGUROS books in this repo.
- Do not change app code, env, emit flags, deploy, or install Automations from a docs-only task.
