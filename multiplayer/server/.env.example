# PostgreSQL (same DATABASE_URL on game server + middleware).
# Required in production (fail-closed). JSON/local fallback is local-dev only.
DATABASE_URL=postgresql://helbreath:helbreath@localhost:5432/helbreath

# Wallet auth (optional in local Development; required in production — fail-closed).
# Must match middleware-node WALLET_AUTH_SECRET in production.
# Login is HMAC session v2 (playerId + actorKind + boundChains). No chain RPC on the game server.
# WALLET_AUTH_SECRET=change-me-in-production
