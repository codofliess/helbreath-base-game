# Agent notes — helbreath-base-game

This repo is the **play / chassis** (browser client + C# server). It is **not** SEGUROS and **not** NA. Do not apply those books here.

## Coding LAWS (fail-closed)

Load the Cursor skill **Coding LAWS fail-closed** (`sand-workflow coding-laws-fail-closed`) before merge-bound work. Pointer only — **do not** copy the book/owner matrix here (desync risk).

Without this file, gates are theater (Cruchi NO-GO).

Hard gates (fail closed):

- `emitir=false` — agents must not move irreversible money.
- Zero secrets in chat.
- Amounts: `Extra=0`.
- One owner per change. Data laws need **book + owner** from the skill (not guessed; not pasted here).
- Wallet / auth **#33** fail-closed: **Cruchi** is the gate; in UI, Phantom is **Martín only**.
- Playtest owner is **Elon**, not the chassis / Cloud Agent.

## Pre-merge

**Bugbot** review and a **Route PR** are required before merge. Do not change game/server code, deploy, or install Automations from a docs-only task.
