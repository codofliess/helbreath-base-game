{
  "name": "chainlords-landing",
  "private": true,
  "version": "1.0.0",
  "description": "Chain Lords marketing landing (static site)",
  "scripts": {
    "start": "node server.js",
    "start:static-only": "serve -s . -l tcp://0.0.0.0:${PORT:-3000}",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
